version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing"
  pre_build:
    commands:
      - echo "Pre Build"
  build:
    commands:
      - echo "Build"
      - npm run build
      - echo "Completed Build project"
  post_build:
    commands:
      - echo "Ending..."
